# Marketplace V2 (Flutter + Python)

Greenfield marketplace project:

- Flutter app (mobile + web): `apps/flutter_app/`
  - Buyer + Seller are both mobile-first UI
- Python backend (FastAPI): `backend/`
- Local infra (Postgres + optional MinIO): `infra/`

## Quickstart (local)

### 1) Backend

See `backend/README.md`.

### 2) Flutter

See `apps/flutter_app/README.md`.

### 3) Local Postgres

See `infra/README.md`.

